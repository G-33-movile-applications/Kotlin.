package com.mobile.mymeds.ui.theme

import androidx.compose.ui.graphics.Color

val LightBluePrimary = Color(0xFF9FB3DF) // Primary color, suitable for accents/buttons
val SkyBlueSecondary = Color(0xFF9EC6F3) // Secondary color
val TealTertiary = Color(0xFFBDDDE4)     // Tertiary color, soft teal/green

// This color looks like the background for the text fields/buttons in the login screen
val CreamBackground = Color(0xFFFFF1D5)

// Common UI colors (kept for general use)
val Black = Color(0xFF000000) // Corrected Black to be fully opaque (0xFF000000)
val White = Color(0xFFFFFFFF)
val DarkGrey = Color(0xFF49454E) // Renamed LightGrey to DarkGrey based on its typical use in Compose themes